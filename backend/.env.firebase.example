# Firebase Admin SDK Configuration (Backend)
# Get service account key from Firebase Console > Project Settings > Service Accounts

# Path to Firebase service account credentials JSON file
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

# Or use environment variables (not recommended for production)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Database Configuration (already exists in settings.py)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ticket_nepal
# DB_USER=postgres
# DB_PASSWORD=your_password
